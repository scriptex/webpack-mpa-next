version: 2.1
jobs:
    build:
        docker:
            - image: cimg/php:8.0.19-browsers
        steps:
            - checkout
            - run:
                  name: autoreconf
                  command: sudo apt install automake
            - run:
                  name: install
                  command: yarn
            - run:
                  name: pwa
                  command: yarn pwa
            - run:
                  name: optisize
                  command: yarn optisize
            - run:
                  name: lint
                  command: yarn lint
            - run:
                  name: prod
                  command: yarn prod
